# LSMC report logo replacement

## Gate 0

- Request: use the supplied new LSMC logo and remove the old LSMC logo as an option.
- Owner: Codex. Branch: `codex/lsmc-new-logo-20260908` in `/Users/jmajor/projects/lsmc/MultiQC-logo-20260908`, based on current `origin/main` commit `400f3de84`. Initial worktree clean.
- The user's existing MultiQC and DYEC work remain separate. An empty DayOA inspection worktree was created at 16.0.89; no DayOA edits are necessary.
- Attachment is a PNG despite its data URI declaring JPEG. Its decoded bytes have SHA256 `d2e52c4eac04778246a160a2d5d35155c6c3198e2352f2c38504244fe2ad16e0`. DayOA's `resources/mqcimg/lsmc_git.jpeg` is already identical to this attachment.
- MultiQC ships an obsolete geometric `lsmc-mark.png` plus two unused `lsmc-compact-*-transparent.png` variants. The presentation loader and replay script reference the old mark; the loader supplies it to the logo, dark logo, favicon and theme icon.
- Scope: replace shipped LSMC branding, remove obsolete image assets, verify rendered reports. Preserve explicitly configured non-LSMC customer branding, including Inflection. Existing published reports/releases are historical and are not rewritten.

| ID | Area | Requirement | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| LOGO-1 | Assets | Install supplied PNG and remove three obsolete LSMC assets | SUCCESS | feature_implementation | Explicit user request | Codex | `lsmc-logo.png` exactly matches attachment hash; asset inventory test | Old assets remained packaged | Three obsolete assets deleted; retained in Git history only |
| LOGO-2 | Presentation | Use new logo for report, print, favicon and theme icon, including replay | SUCCESS | feature_implementation | Explicit user request | Codex | Loader and replay script now reference `lsmc-logo.png`; no old asset references in runtime source | Old default mark | Every built-in theme uses supplied artwork; explicit customer branding remains supported |
| LOGO-3 | Verification | Regression and browser checks for both report templates | SUCCESS | contract_test | Local QA | Codex | 55 tests pass; Ruff and diff checks pass; three rendered HTML pages checked; browser screenshots | | Header/theme icon display correct image; print retains new artwork |

## Verification and disposition

- Command: `/Users/jmajor/projects/lsmc/MultiQC-paginated-20260906/.venv/bin/python -m pytest tests/test_paginated_report.py tests/test_lsmc_report_ui.py -q` from this worktree: **55 passed**.
- `docs/plans/20260908T071400Z_render_logo_preview.py` renders single-page and paginated fixtures. Exact image bytes verified in the single-page header, paginated index, section page, and their theme icons.
- Browser QA using Playwright: `output/playwright/lsmc-logo/paginated.png`, `single.png`, and `print.png`. Single-page logo and theme icon both decoded successfully at native width 834 pixels. Browser print emulation confirms new logo, without asserting broader print-layout correctness.
- No JS/SCSS changed, so compiled bundles do not require rebuilding.
- All three ledger rows are terminal SUCCESS; source implementation and local QA complete. This branch is not published, and existing generated reports are not regenerated by this task.
- DayOA's existing LSMC artwork already matches exactly; no DayOA modification or release is needed for the asset itself.
