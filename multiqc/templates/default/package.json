{
  "name": "multiqc-default-template",
  "version": "1.30.0",
  "description": "Default template for MultiQC reports",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --emptyOutDir",
    "test": "node --test tests/*.test.js",
    "watch": "bash -lc 'source ../../../scripts/openai-env.sh && vite build --emptyOutDir --watch'",
    "dev": "bash -lc 'source ../../../scripts/openai-env.sh && vite build --emptyOutDir --watch'"
  },
  "devDependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8",
    "sass": "^1.99.0",
    "terser": "^5.47.1",
    "vite": "^8.0.12"
  }
}
